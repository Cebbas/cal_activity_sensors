{
  "config": {
    "step": {
      "user": {
        "title": "Create activity sensor",
        "description": "Creates sensors showing whether a filtered event is happening right now / what's next. Leave include/exclude empty for no filtering.",
        "data": {
          "name": "Name",
          "sources": "Source calendars (select multiple)",
          "icon": "Icon",
          "picture": "Picture",
          "field": "Field to match against",
          "include": "Only include events containing (e.g. Zoo, Petting Zoo)",
          "exclude": "Exclude events containing (e.g. Zoo, Private)",
          "use_regex": "Treat the words as regex patterns",
          "case_sensitive": "Case sensitive",
          "trigger_mode": "The sensor should be \"on\" when",
          "create_binary_sensor": "Create binary_sensor (on/off)",
          "create_sensor": "Create sensor (shows current/next event)"
        }
      }
    },
    "error": {
      "no_sources": "Select at least one source calendar",
      "no_sensor_type": "Select at least one sensor type (binary_sensor and/or sensor)"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Edit activity sensor",
        "data": {
          "name": "Name",
          "sources": "Source calendars (select multiple)",
          "icon": "Icon",
          "picture": "Picture",
          "field": "Field to match against",
          "include": "Only include events containing (e.g. Zoo, Petting Zoo)",
          "exclude": "Exclude events containing (e.g. Zoo, Private)",
          "use_regex": "Treat the words as regex patterns",
          "case_sensitive": "Case sensitive",
          "trigger_mode": "The sensor should be \"on\" when",
          "create_binary_sensor": "Create binary_sensor (on/off)",
          "create_sensor": "Create sensor (shows current/next event)"
        }
      }
    }
  },
  "selector": {
    "trigger_mode": {
      "options": {
        "active_now": "An event is happening right now",
        "today": "An event happens at some point today"
      }
    }
  }
}
